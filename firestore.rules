rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read, client writes blocked
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}


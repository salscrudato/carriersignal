rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read, client writes blocked (only Cloud Functions can write)
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}

